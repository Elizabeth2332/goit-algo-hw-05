Порівняння алгоритмів пошуку підрядка: KMP, Боєра-Мура та Рабіна-Карпа
📌 Мета роботи

Дослідити та порівняти ефективність трьох класичних алгоритмів пошуку підрядка:

Кнут–Морріс–Пратт (KMP)

Боєр–Мур

Рабін–Карп

Пошук виконується у двох текстових файлах, а час роботи вимірюється за допомогою timeit.
Для кожного алгоритму проводяться два вимірювання:

Пошук підрядка, який існує у тексті.

Пошук підрядка, якого немає в тексті.

📘 Опис алгоритмів
1️⃣ Кнут–Морріс–Пратт (KMP)

Алгоритм попередньо будує LPS-масив (longest prefix suffix), який дозволяє не повертатися назад у тексті.
Переваги: стабільний, добре працює на будь-якому тексті.
Складність: O(n + m).

2️⃣ Боєр–Мур

Порівнює символи з кінця підрядка та використовує таблицю зсувів.
Переваги: дуже швидкий на великих текстах, робить "стрибки".
Складність: середня O(n / m), найгірша O(nm) — але на практиці це рідко.

3️⃣ Рабін–Карп

Обчислює хеш підрядка і порівнює його з хешами частин тексту.
Переваги: ефективний при пошуку багатьох підрядків.
Недоліки: можливі зіткнення, перехешування.
Складність: O(n + m) середня, O(nm) найгірша.

🧪 Методика експерименту

Для аналізу:

Завантажено два текстових файли: article1.txt та article2.txt.

Обрано по два патерни для кожного тексту:

існуючий підрядок

вигаданий підрядок, якого точно немає в тексті

Використано функцію measure_time() з timeit для багаторазового виконання алгоритмів.

Кожен алгоритм запускався однакову кількість разів (100 повторів).

📊 Результати вимірювань